name: CI

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macOS-latest,windows-latest,ubuntu-latest]
    steps:
      - uses: actions/checkout@v1
      - name: yarn install
        run: yarn
      - name: Run yarn build
        run: yarn electron:build
